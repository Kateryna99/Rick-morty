export enum ProgressValue {
    announceDate = 22,
    mainLoader = 100,
}

export enum ProgressSpeed {
    announceDate = 150,
    mainLoader = 40,
}

export enum ProgressStart {
    announceDate = 1,
    mainLoader = 0,
}